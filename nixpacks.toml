[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["rm -rf .next", "npm run build"]
env = { SKIP_ENV_VALIDATION = "1" }

[start]
cmd = "npm start"
